<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihedgefund.mapper.CoinTransactionMapper">

    <resultMap id="txMap" type="com.aihedgefund.model.DO.CoinTransactionDO">
        <id     property="id"           column="id"/>
        <result property="userId"       column="user_id"/>
        <result property="amount"       column="amount"/>
        <result property="type"         column="type"/>
        <result property="reason"       column="reason"/>
        <result property="balanceAfter" column="balance_after"/>
        <result property="createdAt"    column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO coin_transactions (user_id, amount, type, reason, balance_after, created_at)
        VALUES (#{userId}, #{amount}, #{type}, #{reason}, #{balanceAfter}, datetime('now'))
    </insert>

    <select id="selectByUserId" resultMap="txMap">
        SELECT id, user_id, amount, type, reason, balance_after, created_at
        FROM coin_transactions
        WHERE user_id = #{userId}
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM coin_transactions WHERE user_id = #{userId}
    </select>

</mapper>
